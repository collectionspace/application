package org.collectionspace.chain.config.main.csp;

// XXX will get some context object or something passed, at allow more computation
public interface CSPConfigEvaluator {
	public Object getValue();
}
